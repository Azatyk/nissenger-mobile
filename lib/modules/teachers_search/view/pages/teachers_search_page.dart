import 'package:flutter/material.dart';
import 'package:nissenger_mobile/modules/teachers_search/view/components/search_bar.dart';

class TeachersSearchPage extends StatelessWidget {
  const TeachersSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Padding(
          padding: const EdgeInsets.symmetric(horizontal: 20),
          child: Container(),
        ),
      ),
    );
  }
}
